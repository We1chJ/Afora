/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.clerk.com', 'images.pexels.com', 'pexels.com'],
    },
};
// next.config.js

export default nextConfig;
